# OneAuth 部署环境变量

# 安装目录
INSTALL_DIR=/opt/oneauth

# Gateway 对外端口
GATEWAY_PORT=80

# 数据库
DB_NAME=sso_platform
DB_USER=sso
DB_PASSWORD=Password123@sqlite

# Redis
REDIS_PASSWORD=Password123@redis

# SSO 平台对外地址（用于 OIDC issuer 与回调）
SSO_ISSUER=http://localhost
SECRET_KEY=oneauth-secret-key-default
